# Copie para secrets.toml (local) ou configure em Streamlit Cloud → Settings → Secrets
#
# Proteção opcional por senha — remova a seção [dashboard] para deixar aberto.

[dashboard]
senha = "sua_senha_aqui"
